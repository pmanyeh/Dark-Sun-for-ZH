# 《黑暗太陽》(Dark Sun: Shattered Lands) 中文化專案進度總結

這份文件記錄了目前為止中文化探索的進度與已知的技術情報，方便未來重啟專案時能快速接軌。

> **2026-08-10 更新**：本文件以下內容寫於 7/14，已經過時。之後（8/10）有大量逆向工程進展，**請先看 `docs/re/re_07_verified_gpl_loop_and_dosbox_setup.md`**——裡面記錄了真正用 Ghidra 反組譯驗證過的 GPL 執行迴圈格式、一份會讓遊戲在 DOSBox-X 裡正常開機的原廠設定檔、以及用截圖驗證過的真實遊戲對話。`docs/re/re_01` ~ `re_06` 裡有部分「已完全破譯」的結論**未經驗證、後來證實是誤判**，請以 `re_07` 為準。
>
> **2026-08-10 第二次更新（延續 re_07 的低風險支線）**：`re_07` 建議的兩條路線中，「動態除錯追出 overlay 分派表真實位址」需要人在場手動操作 DOSBox-X 除錯器（按熱鍵截圖、在除錯器主控台輸入指令），不是能在無人值守的命令列環境裡自動完成的事，所以這次改為推進第二條「風險低、現在就能做出成果」的支線——**NAME/SPIN 純文字抽取、翻譯、與封裝管線**，細節見下方新增的「6. 封裝管線驗證與翻譯成果」章節。
>
> **2026-08-10 第三次更新（人在場、實際操作 DOSBox-X 除錯器完成動態追蹤）**：接續上面的高風險支線，這次真的在除錯器裡人在場逐步操作，成功用 `BPINT 21 3D` 抓到遊戲開啟 `GPLDATA.GFF` 的那一刻，並沿呼叫堆疊往上追了 5 層，**實際觀察並確認了 Borland overlay manager 的 `int 3Fh` 呼叫樁、執行期動態填值的遠跳分派表，還把一個已載入 overlay 的記憶體內容比對回 `DSUN.EXE` 檔案，反推出精確的檔案位移（`0x62030`）**。也算出一個下次可以優先驗證的假設位址（`5B7C:5FD0`，推測可能是 `FUN_0000_5160` GPL 迴圈的執行期位置）。詳見新文件 `docs/re/re_08_overlay_manager_dynamic_trace.md`。

---

## 1. 專案目標與初始環境
*   **目標**：將 Steam 版的《浩劫殘陽》(Dark Sun: Shattered Lands) 進行文本中文化。
*   **遊戲路徑**：`D:\git\Dark Sun Series\from Steam\games\Dark Sun-ENG\GAME\DARKSUN`
*   **資源檔格式**：遊戲的主要資源皆打包在多個 `.GFF` 檔案中（例如 `GPLDATA.GFF`, `RESOURCE.GFF`, 以及各地圖的 `RGN*.GFF`）。

## 2. 目前已完成進度 (What We Did)
1.  **編譯解包工具**：
    *   成功引入了開源專案 [JohnGlassmyer/dsun_music](https://github.com/JohnGlassmyer/dsun_music)。
    *   在背景部署了 JDK 17 與 Maven，並順利將原始碼編譯為可執行的 `gff-tool.jar`。
2.  **全面資源解包**：
    *   使用 `gff-tool` 將遊戲內共 **41 個 `.GFF` 檔案** 成功解包。
    *   解包結果已全數存放於：`D:\git\dsun_extracted`。

## 3. 已取得的資料與分析 (What We Got)
透過對解包資料 `D:\git\dsun_extracted` 的盤點，我們確認了文本資源散落在不同類型的「標籤 (Tag)」中：

| 檔案標籤 (Tag) | 存放位置 (舉例) | 內容說明 | 狀態 |
| :--- | :--- | :--- | :--- |
| **`NAME`** | `GPLDATA\NAME-*` | **裝備與物品名稱**（如 Long Sword, Chest Armor）。 | 🟢 可讀純文字 |
| **`SPIN`** | `RESOURCE\SPIN-*` | **法術與特殊物品詳細說明**。共約 255 個檔案，因 8-bit 遊戲上限而定。 | 🟢 可讀純文字 |
| **`TEXT`** | `RESOURCE\TEXT-*` | **短字串**，例如 NPC 名字、簡短的 UI 提示。 | 🟢 可讀純文字 |
| **`GPL`** | `GPLDATA\GPL-*` <br> `RGN*\GPL-*` | **遊戲劇情腳本與對話樹**。這是遊戲文本的核心！高達數百個檔案。 | 🔴 **二進位/加密壓縮** |
| **`FONT`** | `RESOURCE\FONT-*` | **遊戲字庫檔**。掌管遊戲畫面的文字繪製。 | 🔴 特殊圖形格式 |

## 4. 目前遭遇的技術瓶頸 (Blockers)
*   **缺乏 GPL 反編譯工具**：真正的劇情文本被寫死在二進位腳本 (`GPL`) 中，且目前開源社群 **不存在** 現成的 Dark Sun GPL 腳本反編譯器。
*   **字串非明文**：GPL 內的字串無法透過一般的 Hex 編輯器或字串搜尋工具 (如 `strings`) 找出，這表示文字已經被**壓縮**或採用了**自定義的字元編碼表**。
*   **中文字型支援 (雙位元組問題)**：原版遊戲僅支援 ASCII 單字節字型，若要顯示中文，必須對主程式 (`DSUN.EXE`) 進行修改以支援 Big5/GBK 等雙位元組字元，並修改 `FONT` 檔。

## 5. 下一步建議 (Next Steps)
若未來要繼續推動這個硬核的漢化專案，建議從以下幾個技術方向著手：

1.  **初步練手（純文字替換）**：
    *   使用純文字編輯器先試著翻譯 `NAME` 或 `SPIN` 裡面的字串。
    *   使用 `gff-tool` 的 `--replace-with` 功能將翻譯好的檔案封包回 `.GFF` 中。
    *   進入遊戲測試是否能正常顯示英文字母的變更（確認重新封包的邏輯無誤）。
2.  **逆向工程：尋找字元表 (Character Table)**：
    *   使用 **Cheat Engine** 或 **DOSBox Debugger**，在遊戲顯示劇情對話時追蹤記憶體位址。
    *   尋找螢幕緩衝區 (Screen Buffer) 內的數值，藉此反推出遊戲自定義的字元對應表。
3.  **主程式逆向分析 (ROM Hacking)**：
    *   使用 **Ghidra** 或 **IDA Pro** 載入 `DSUN.EXE`。
    *   尋找讀取 `.GPL` 檔案以及呼叫字型繪製 (Font Rendering) 的組合語言函式，研究其解壓縮邏輯並嘗試掛載中文外掛字庫。
4.  **關注社群動態**：
    *   定期查看 Github 或 Gold Box Games 論壇，是否有大神發布了針對 SSI 引擎的腳本解譯器。

---

## 6. 封裝管線驗證與翻譯成果（2026-08-10 第二次進度）

### 6.1 `gff-tool` 封裝管線已完整驗證（無損、支援任意長度替換）
用 `D:\git\dsun_music\gff-tool\target\gff-tool.jar` 對 `RESOURCE.GFF` 做了三組測試（皆在暫存複本上進行，**沒有動到 Steam 原始遊戲檔**）：
1. **無修改回封裝**：用 `--replace-with` 把某個資源換成它自己，重新封裝後的檔案 MD5 跟原始檔**完全一致** → 封裝邏輯是無損的。
2. **替換成較短的繁體中文（Big5 編碼）**：確認能正確寫回，且不影響檔案裡其他鄰近資源的位移量。
3. **替換成超過原長度的繁體中文**：確認工具會把新內容附加到檔案尾端、正確更新索引表，其他資源依然完好。

**結論**：封裝管線可以安全地把任意長度的翻譯文字寫回遊戲檔案。技術上的瓶頸只剩「遊戲主程式的字型只支援單位元組 ASCII」這一項（見第 4 節）——就算現在把中文塞進去，畫面還是顯示不出正確字形，這點還沒解決。

### 6.2 SPIN（法術/特殊物品說明）：172 則全數翻譯完成，且已做過整批封裝驗證
* `RESOURCE.GFF` 裡的 `SPIN-0001` ~ `SPIN-0180` 每一個檔案都是**獨立完整的一筆 GFF 資源**（不像 NAME，見 6.3），可以逐筆用 `--replace-with` 安全地個別替換，不會動到其他資源的結構。
* 172 則法術/心靈能力說明（AD&D 2nd edition 法術，含 Dark Sun 心靈能力系統）**已全數翻譯成繁體中文**，存於：
  * `localization/SPIN_spells_translated.json`（英中對照，含來源 GFF tag/number）
  * `localization/spin_zh_big5/SPIN-NNNN`（Big5 編碼、CRLF 結尾，可直接丟給 `gff-tool --replace-with` 使用的成品檔）
  * `SPIN-0173` ~ `SPIN-0180` 這 8 個編號在遊戲檔案裡根本不存在（可能是保留位），不算漏翻。
* **已做過整批封裝回歸測試**：把全部 172 則翻譯依序用 `--replace-with` 寫回一份測試用 `RESOURCE.GFF` 複本，重新解包後逐一位元組比對——172/172 完全吻合翻譯來源；除了資源索引表 `GFFI-01`（本來就該因位移量變動而改變）之外，**檔案裡其他所有資源都跟原始檔案一模一樣**，證實這套翻譯可以整批安全套用，不會波及其他資料。
* 翻譯狀態：Claude 的第一版翻譯，採用常見 AD&D/TRPG 中文譯名慣例（例如 Fireball＝火球術、Magic Missile＝魔法飛彈、Hold Person＝定身人類術），**尚未在遊戲畫面裡實際核對**（因為字型瓶頸還沒解決，塞進去現在也顯示不出正確中文）。

### 6.3 NAME（裝備/場景物件名稱）：328 個名稱已抽取翻譯，但**還不能直接封裝**
* `GPLDATA.GFF` 裡的 `NAME-1` 是**一整筆資源裡塞了全部物件名稱**，而且記錄格式還沒被反組譯出來（名稱字串之間夾雜著看起來像「重複使用未清空的緩衝區殘留內容」的垃圾位元組，例如反覆出現的 `...NAMEIX\*.oda` 路徑片段）。目前是用正規表示式在原始位元組裡抓出 Title-Case 的英文片段，抓到 328 個名稱（詳見 `localization/NAME_objects_translated.json`），但**還不知道遊戲引擎讀取這筆資源時實際使用的欄位寬度/步進值**。
* **重要：跟 SPIN 不同，NAME-1 絕對不能直接拿翻譯後的純文字去覆蓋整包重新封裝**——因為不確定遊戲是用固定位移量（index × 記錄大小）去存取每個名稱，貿然覆蓋很可能打亂記錄邊界，讓其他名稱、甚至圖示 ID 對應錯亂。這份翻譯先當作「翻譯字典」存起來，等之後用 Ghidra 反組譯出 NAME 記錄的真實 struct 格式後再套用。
* 328 個名稱裡有 8 個（id 24、25、86、87、88、90、117、223）明顯是抽取時被截斷/切錯的片段（例如 "Trail" / "Blood" 應該合併回 "Trail of Blood"），JSON 裡都用 `note` 欄位標記出來了，需要人工核對遊戲脈絡後修正。
* 人名/專有名詞（Nagi、Dagolar、Phrain、Derth、El、Balk、Grey、Tanelyv、Dapartea、Degula、Koeatl、Keldar、Tynan、A'poss、Tristram、Mirlon、Llod 等）目前採音譯，之後 GPL 對話文本破解出來後，**要確保跟對話裡出現的同一批人名譯名保持一致**。

### 6.4 尚未處理：TEXT（NPC 短名字）
* `RESOURCE.GFF` 裡的 `TEXT-000` ~ `TEXT-059`（60 個檔案）內容都是極短的 NPC 人名（例如 "Deestan"、"Garn"、"Dag"），這次沒有翻譯，因為人名音譯最好留到跟 GPL 對話文本裡出現的同一批角色名字一起處理，才能保持全專案譯名一致，避免各自為政。

### 6.5 下一步建議（更新版）
1. **低風險、可持續推進**：SPIN 已經全翻完且封裝驗證過，剩下主要是等字型雙位元組問題解決後即可直接套用；也可以趁現在先把 TEXT 的 60 個 NPC 短名字音譯掉，或找人工核對 SPIN 翻譯的準確度。
2. **中風險**：反組譯 `NAME-1` 的記錄格式（欄位寬度、跟圖示 ID 的對應關係），把已翻好的 328 個名稱字典安全套用回去。
3. ~~**高風險/需要人在場**：`re_07` 提到的「用 `BPINT 21 3D` 鎖定遊戲真正讀取 `GPLDATA.GFF` 的那一刻，回推 overlay 分派表真實位址」——這步需要在 DOSBox-X 除錯器裡手動下指令、逐步執行，適合下次有人在場、可以互動操作除錯器時再進行。~~ **已完成**（2026-08-10 第三次更新）：實際抓到斷點、追出呼叫鏈、確認 overlay manager 機制並對回檔案位移，細節見 `docs/re/re_08_overlay_manager_dynamic_trace.md`。
4. **新的高風險/需要人在場（`re_08` 交棒）**：`re_08` 第 5.1 節算出一個假設位址 `5B7C:5FD0`，推測可能是 `FUN_0000_5160`（GPL 記錄執行迴圈）的執行期實際位置——**但這個猜測還沒驗證過**。下次重啟同一套環境、走到同一個開檔斷點後，第一件事應該是檢查這個位址的反組譯內容像不像 `FUN_0000_5160` 的開頭；如果猜錯，就要改用 `re_08` 5.2 節建議的方法（放行開檔斷點、改追後續讀檔或 overlay 載入事件）。
5. **核心瓶頸**：雙位元組中文字型支援，需要對 `DSUN.EXE` 常駐段做 ROM hack，這塊完全還沒開始動工。
